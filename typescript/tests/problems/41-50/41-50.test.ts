import {
  answers,
  problem41,
} from '../../../src/problems/41-50'

describe('Problems 41-50', () => {
  test.skip('problem41 should return expected result', () => {
    const result = problem41()
    expect(result).toStrictEqual(answers[0])
  })
})
