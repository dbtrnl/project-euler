import { expect } from 'chai'
import { answers, problem95 } from '.'

describe('Problems 91 to 100', () => {
  it('problem95', () => {
    // const result = problem95()
    const result = undefined
    expect(result).to.be.equal(answers[0])
  })
})
